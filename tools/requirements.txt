# For tools/analyze_beats.py and tools/verify_beats.py. ffmpeg must also be on
# PATH - it is what decodes the m4a.
#
#   python3 -m venv .venv-beat
#   .venv-beat/bin/pip install -r tools/requirements.txt
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12
